 编程题 

  使用基于 tcp 协议的编程模型实现多人同时在线聊天和传输文件，要求每个客户端将发 送的聊天内容和文件发送到服务器，
 服务器接收到后转发给当前所有在线的客户端。

作业：
1.客户端想让用户选择是发送消息还是传输文件，1为发送消息，2为传输文件；选择后先发送一个字符串，选择1则发送message,2为file;
再发送对应的消息或则文件
2.服务端接收到客户端发送过来的字符串是message,还是file，选择是BufferReader还是用ObjectInputStream来接收数据，最后再发送
给当前连接上服务器的客户端（这个功能没有实现）。